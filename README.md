This app uses BFS (Breadth-First Seatch) in order to traverse a 8x8 chess board as a Knight. Usage is knightMoves(startposition, targetposition). The console will print the shortest possible route(s)
